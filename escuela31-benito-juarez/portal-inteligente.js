document.addEventListener("DOMContentLoaded",()=>{function bind(inputId,containerId,emptyId){const input=document.getElementById(inputId),box=document.getElementById(containerId),empty=document.getElementById(emptyId);if(!input||!box)return;const cards=[...box.children].filter(x=>x.matches("article"));const run=()=>{const q=input.value.toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"").trim();let visible=0;cards.forEach(c=>{const hay=(c.dataset.search+" "+c.textContent).toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g,"");const show=!q||hay.includes(q);c.hidden=!show;if(show)visible++;});if(empty)empty.hidden=visible>0;};input.addEventListener("input",run);}bind("librarySearch","libraryItems","libraryEmpty");bind("serviceSearch","serviceItems","serviceEmpty");});